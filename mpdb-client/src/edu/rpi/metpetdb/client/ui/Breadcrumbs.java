/*
 * package edu.rpi.metpetdb.client.ui;
 * 
 * import java.io.File;
 * 
 * 
 * public class Breadcrumbs { /* this.bar = new
 * BreadcrumbBar(TestImageCreator.getListInnerIcon(), ":", new DirCallback());
 * 
 * 
 * File[] roots = File.listRoots(); BreadcrumbItem[] bRoots = new
 * BreadcrumbItem[roots.length]; int count = 0; for (File root : roots) { String
 * systemName = fsv.getSystemDisplayName(root); if (systemName.length() == 0)
 * systemName = root.getAbsolutePath(); BreadcrumbItem rootItem = new
 * BreadcrumbItem(new String[] { systemName, root.getAbsolutePath() }); Icon
 * icon = fsv.getSystemIcon(root); rootItem.setIcon(icon); bRoots[count++] =
 * rootItem; }
 * 
 * bar.pushChoices(new BreadcrumbItemChoices(bRoots));
 * 
 */
/*
 * File[] roots = File.listRoots(); BreadcrumbItem[] bRoots = new
 * BreadcrumbItem[roots.length]; }
 */